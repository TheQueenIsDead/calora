version: '3'

tasks:
  filter:
    desc: "Filter OFF dump to APAC CSV using DuckDB, then delete dump"
    generates:
      - off/apac.en.openfoodfacts.org.products.csv
    cmds:
      - uv run python filter.py
      - rm off/en.openfoodfacts.org.products.csv.gz
